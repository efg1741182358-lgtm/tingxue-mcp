# 安全说明

听雪操作的是一个**真人的网易云账号**，不是沙盒。所以安全边界不是加分项，
是这个项目能不能给别人用的前提。

发现问题请开 issue。如果是尚未公开的漏洞，issue 里写清影响面即可，
不必附带可直接复用的攻击载荷。

## 威胁模型

假设攻击者知道你的服务地址（这不难：`*.zeabur.app` 之类的域名会出现在
公开的证书透明日志里，扫描是自动化的、无差别的），但不知道你的
`AUTH_PASSWORD`。在这个前提下，他不应该能够：

- 调用任何 MCP 工具
- 更换服务端正在使用的网易云账号
- 拿到任何形式的访问令牌
- 读到登录 Cookie

`AUTH_PASSWORD` 是唯一的门。不设置它就等于没有门——服务会在启动时告警，
并且未设置口令时扫码登录只允许本机访问。

## 已处理

### 2026-08-28：扫码登录入口未鉴权

`AUTH_PASSWORD` 原来只守住了 `/mcp`。`/login`、`/login/qr`、`/login/check`
是公开的，任何知道地址的人都能拉一个二维码、用**自己的**网易云账号扫，
再调 `/login/check`——服务端会保存他的登录态，顶掉部署者的。

后果不是「Cookie 被偷走」，是反过来：这台服务开始替陌生人的账号干活，
而部署者的助手仍以为在操作自己的账号。**一个只守住 `/mcp` 的口令，
守的是「谁能指挥它」，没守住「它是谁」。**

现在三个端点都要求管理身份：同一个 `AUTH_PASSWORD` 换一张 30 分钟的
httpOnly cookie；未设置口令时只放行回环地址。

### 2026-08-28：授权码未绑定客户端与回调地址

动态注册会发 `client_id` 但不保存，授权阶段也不校验 `redirect_uri`。
任何人都能构造一条**指向部署者自己域名**的授权链接，把 `redirect_uri`
填成自己的服务器。受害者看到的是熟悉的域名和熟悉的口令框，输入口令后，
授权码直接送到攻击者手里，换成长期令牌。**全程不需要知道口令。**

现在：注册信息会保存，`authorize` 逐字匹配 `redirect_uri`，授权码绑定
`client_id` + `redirect_uri`，换令牌时再核验一次，不匹配一律 400 且
**绝不重定向**。PKCE 强制 `S256`，发现文档不再宣告 `plain`。

令牌加了 `purpose` / `iss` / `aud` / `cid`；管理 cookie 与 access token
用途隔离，互相不能顶用。有效期从一年改为 30 天。

**吊销全部令牌的方法：改掉 `AUTH_PASSWORD`。** 签名密钥从口令派生，
口令一变，所有已签发的令牌当场失效。启动日志里的口令指纹就是给这件事用的。

### 2026-08-28：登录态落盘

目录 `0700`、文件 `0600`，先写临时文件再 `rename`（原子），进程写到一半
被杀不会留下半截 JSON。

### 2026-08-28：`REAL_IP` 不再兜底

原来没配就用一个写死的示例地址。那意味着每一份忘了配的部署都从同一个 IP
出站，那个地址迟早被限流，然后所有人一起挂、各自查不出原因。而且伪造出口
IP 这件事，使用者有权知道自己在做。现在不配就不注入，并在启动时说明后果。

## 依赖告警的处置结论

`npm audit` 当前报告 3 条（1 moderate、2 high），链路是：

```
NeteaseCloudMusicApi → music-metadata → file-type
```

漏洞本身是 `file-type` 在解析畸形 ASF 文件时可能陷入死循环
（[GHSA-5v7r-6r5c-r473](https://github.com/advisories/GHSA-5v7r-6r5c-r473)）。

**结论：不可达。** 依据：

1. 上游包里引用 `music-metadata` 的只有一个模块——`module/cloud.js`（网盘上传）。
   可以自行复核：`grep -rl music-metadata node_modules/NeteaseCloudMusicApi`
2. 听雪调用的上游接口共 23 个，逐一列在 `src/netease.js` 里，其中没有 `cloud`。
3. 触发条件是解析本地音频文件。听雪不接受、不读取、不上传任何音频文件。

**因此不做 `npm audit fix --force`**：那会把 `NeteaseCloudMusicApi` 降到
3.x，是破坏性变更，用一个真实的功能回退去换一个够不到的告警，不划算。

**但这个结论有有效期。** 一旦以后新增了会走 `cloud` 相关接口的工具，
这条结论立刻作废，必须重新评估。加工具的人有义务回来改这一段。

## 部署检查清单

- [ ] `AUTH_PASSWORD` 已设置，且足够长
- [ ] `PUBLIC_URL` 与实际域名一致（首尾无空格）
- [ ] 持久卷已挂到 `/app/data`
- [ ] 境外部署已设置 `REAL_IP`
- [ ] 用 `TOOLS` 只开真正需要的工具组——**不需要的能力不要给出去**，
      这比事后指望模型自觉可靠得多
- [ ] 打开 `https://你的域名/login`，确认在没有口令的浏览器里看到的是
      口令框，而不是二维码
